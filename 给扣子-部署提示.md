# 给扣子的部署任务

请将这份 ZIP 解压后，把 `russia-music-platform/` 目录作为完整的 Next.js 项目源码部署到 Vercel。项目根目录是包含 `package.json`、`package-lock.json`、`vercel.json` 和 `app/` 的目录；不要把 ZIP 外层目录设为 Root Directory。

## 目标

1. 请新建独立的 Vercel 项目，建议名称 `russia-music-gift`。不要覆盖现有的 `russia-music-platform-psdr`、`russian-music-history-map` 或其他生产项目。如果必须关联 Git 仓库，请新建仓库或隔离分支并先给出预览部署。
2. 使用 Node.js 20、Next.js 框架。无需数据库或环境变量。使用 `npm ci` 安装依赖，用 `npm run build` 构建；不要上传 `node_modules` 或 `.next`。
3. 先创建 Preview 部署并给出实际 URL、构建状态和构建日志关键结果。未经确认，不要提升为 Production 或修改现有域名。
4. 打开预览地址，检查首页 `/`、院校 `/schools`、申请工具 `/my-application`、曲库 `/sheet-music`、音乐史 `/music-history`。再在院校详情中点击“收藏”和“加入目标”，确认申请工具同步显示。手机宽度也请检查导航和卡片是否溢出。
5. 如果远程构建失败，请根据构建日志修复源码，重新运行 `npm run check` 与 `npm run build` 后再次提交预览部署。不要把失败的构建报告为上线成功。

## 已经完成的本地验证

- `npm run check` 通过。
- 在 Node.js 24 的工作环境中执行 `npm run build` 通过，Next.js 15.5.24 编译成功并生成 93 个静态页面；部署配置固定使用 Node.js 20，仍以 Vercel 远程构建结果为准。
- 本站“我的申请”数据存于访客浏览器的 `localStorage`，换设备不会自动同步。

如果扣子只能接收压缩包，直接上传这一份 ZIP；解压后按上述根目录部署即可。
